public:
bool searchMatrix(vector<vector<int>> &matrix, int target)
{
  //         using binary search
  int low = 0;
  int high = matrix[0].size() - 1;
  while (low <= high)
  {
    int mid = low + (high - low) / 2;
    if (matrix[i][])
  }

  return true;
}
}
;